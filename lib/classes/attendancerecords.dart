class Attendancerecords {
  final String grade;
  final String section;
  final String subject;
  final int present;
  final int absent;
  final int leave;
  final DateTime starttime;
  Attendancerecords(this.grade, this.section, this.subject, this.present,
      this.absent, this.leave, this.starttime);
}
